{
  "name": "gemini-event-creator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && copy manifest.json dist\\ && copy content.css dist\\ && copy popup.html dist\\ && copy popup.js dist\\ && xcopy services dist\\services\\ /E /I",
    "build:extension": "npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
