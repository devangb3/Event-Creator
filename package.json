{
  "name": "gemini-event-creator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && cp manifest.json dist/ && cp content.css dist/ && cp -r services dist/",
    "build:extension": "npm run build"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
